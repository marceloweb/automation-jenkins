node {

  try {
      stage('Preparation') {
         checkout scm
      }

      stage('Build') {
         sh "cd ../../ansible/ && ansible-playbook configuring.yml -i hosts"
      }

    } catch(e) {
       throw e;
   }                                     
}       
