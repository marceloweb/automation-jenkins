node {

  try {
      stage('Preparation') {
         checkout scm
      }

      stage('Build') {
         ansiblePlaybook inventory: '../../ansible/hosts', playbook: '../../ansible/provisioner.yml' 
      }

    } catch(e) {
       throw e;
   }                                     
}       
